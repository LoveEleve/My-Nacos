# 手写 Nacos 问题驱动指南

> 从根本问题出发，一层层拆解为什么需要 Nacos，以及它如何解决这些问题

---

## 如何阅读本文档

本文档采用 **「问题驱动的逆向工程学习」** 方法。

### 核心理念

```
理解深度 = 提问质量 × 推导链长度
```

不是要记住 Nacos 的代码细节，而是要**还原设计决策过程**——问自己：如果我是设计者，面对这个问题会怎么解决？

---

### 五步提问法

每章都遵循这个思考框架：

| 步骤 | 问题 | 目的 |
|------|------|------|
| **Step 1** | 这是什么？ | 观察到的现象 / 现状 |
| **Step 2** | 为什么需要这个？ | 解决的问题 / 痛点 |
| **Step 3** | 有什么替代方案？ | 还可以怎么做 / 对比 |
| **Step 4** | 这样设计有什么代价？ | 权衡 / 取舍 |
| **Step 5** | 如果我来设计，会怎么做？ | 迁移思考 / 举一反三 |

---

### 问题金字塔

```
                    ┌───────────┐
                    │  为什么？  │  ← 本文档重点（设计原理）
                    │   Why?    │
                    ├───────────┤
                  ┌─┴───────────┴─┐
                  │   怎么做？    │  ← 实现方式 / 数据结构
                  │    How?       │
                  ├───────────────┤
                ┌─┴───────────────┴─┐
                │    是什么？       │  ← 简要介绍（不过多停留）
                │    What?          │
                └───────────────────┘
```

**理解深度：为什么 > 怎么做 > 是什么**

---

### 推导链示例

以第1章为例，展示如何层层深入：

```
Q1: Nacos 客户端启动时需要知道服务端地址，地址写在哪？
    A1: 可以写死在代码里
        ↓
    Q2: 写死有什么问题？
        A2: 注册中心扩容/迁移时需要改代码重新部署
            ↓
        Q3: 那应该写在哪？
            A3: 配置文件、环境变量、JVM参数都可以
                ↓
            Q4: 多个来源冲突了怎么办？
                A4: 定义优先级：JVM参数 > 环境变量 > 配置文件
                    ↓
                Q5: 生产环境有多个 Nacos 节点，怎么选？
                    A5: 随机选择 + 失败重试 + 成功记录
                        ↓
                    Q6: 这和 DNS 有什么区别？
                        A6: DNS 是域名解析，Nacos客户端需要感知多个实例的健康状态

推导链长度：6层
理解深度：从"配置写在哪"到"理解服务发现客户端的设计哲学"
```

---

### 阅读建议

1. **不要跳读** —— 每章都建立在前一章的基础上
2. **先自己思考** —— 看到问题先停下来想5分钟，再往下看答案
3. **画推导图** —— 用纸笔或思维导图记录问题链
4. **完成检查清单** —— 每章结尾的 checklist 必须能回答出来

---

## 第0章：为什么要有服务注册中心？

### 问题：微服务之间怎么找到对方？

假设你有一个电商系统：
- 订单服务需要调用用户服务查询用户信息
- 订单服务需要调用库存服务扣减库存
- 订单服务需要调用支付服务发起支付

**最简单的方案**：把服务地址写死在代码里

```java
// 订单服务调用用户服务
String userServiceUrl = "http://192.168.1.100:8080";
User user = restTemplate.getForObject(userServiceUrl + "/user/" + userId, User.class);
```

**这会引发什么问题？**

| 问题 | 后果 |
|------|------|
| 服务扩容/缩容时 IP 变化 | 必须改代码重启 |
| 服务宕机后调用失败 | 没有自动剔除故障节点 |
| 多实例负载均衡 | 需要自己实现 |
| 服务上线/下线感知 | 调用方无感知 |

**本质上**：服务调用方需要一种机制，能**动态获取**服务提供方的地址，并且能感知**地址的变化**。

**解决方案**：引入一个"中介"——服务注册中心

```
┌─────────────┐      注册/心跳      ┌─────────────────┐      查询/订阅      ┌─────────────┐
│  订单服务    │ ──────────────────> │  服务注册中心     │ <────────────────── │  用户服务    │
│  (调用方)   │                     │  (Nacos/Eureka) │                     │  (提供方)   │
└─────────────┘                     └─────────────────┘                     └─────────────┘
                                           ↑
                                           │ 主动推送变更
                                           ↓
```

**服务注册中心的核心职责**：
1. **服务注册**：提供方启动时告诉注册中心"我在这里"
2. **服务发现**：调用方从注册中心获取提供方地址
3. **健康检查**：检测提供方是否还活着
4. **变更通知**：提供方变化时通知调用方

---

## 第1章：客户端启动时怎么找到注册中心？

### 问题：注册中心本身的地址写在哪？

服务实例要注册，必须先知道注册中心在哪。但这个地址本身也是一个配置。

**最简单的方案**：写死在代码里

```java
// 创建 Nacos 客户端时硬编码地址
NacosClient client = new NacosClient("192.168.1.100:8848");
```

**这会引发什么问题？**

| 问题 | 后果 |
|------|------|
| 注册中心扩容/迁移 | 必须改代码重新打包部署 |
| 多环境配置不同 | 开发/测试/生产需要不同版本 |
| 高可用部署 | 单点故障，没有备用地址 |
| 动态切换 | 注册中心故障时无法自动切换 |

**本质上**：需要一个**灵活的配置机制**，支持多种配置来源，并且能**动态选择可用节点**。

**解决方案：分层配置体系**

```
配置优先级（从高到低）：
1. JVM 启动参数（-DserverAddr=xxx）
2. 环境变量（SERVER_ADDR=xxx）
3. 配置文件（application.properties）
4. 默认值（localhost:8848）
```

**为什么这样设计？**

| 配置来源 | 适用场景 | 优先级高的原因 |
|----------|----------|----------------|
| JVM 参数 | 临时调试、测试 | 最灵活，启动时可变 |
| 环境变量 | 容器化部署（K8s）| 镜像不变，配置外置 |
| 配置文件 | 传统部署 | 易于版本管理 |
| 默认值 | 本地开发 | 开箱即用 |

**数据结构**：

```java
public class NacosClientProperties {
    // 按优先级排序的配置源列表
    private List<PropertySource> propertySources;
    
    public String getProperty(String key) {
        // 按优先级遍历，第一个非空值即为结果
        for (PropertySource source : propertySources) {
            String value = source.getProperty(key);
            if (value != null) return value;
        }
        return defaultValue;
    }
}
```

**多地址与负载均衡**

生产环境通常部署多个 Nacos 节点：

```properties
serverAddr=192.168.1.100:8848,192.168.1.101:8848,192.168.1.102:8848
```

**选择策略**：

```
1. 随机选择一个地址尝试连接
2. 连接失败则尝试下一个
3. 记录成功的地址，优先使用
4. 定期探测失败地址，恢复后重新加入候选
```

**演进：从硬编码到服务发现**

| 阶段 | 方式 | 说明 |
|------|------|------|
| 硬编码 | 代码里写死 IP | 最早期的做法 |
| 配置文件 | properties/yaml | 主流方式 |
| 环境变量 | 容器时代 | 12-factor 原则 |
| DNS | 域名解析 | 自动负载均衡 |
| 服务网格 | Sidecar 代理 | 对应用透明 |

**本质上**：注册中心地址的管理，体现了**配置外部化**的演进趋势——把易变的东西从代码中剥离出来。

---

## 第2章：服务存在哪？

### 问题：服务端怎么存储服务信息？

假设有100个服务，每个服务有10个实例，怎么存？

**最简单的方案**：用一个 List

```java
List<Instance> instances = new ArrayList<>();
```

**这会引发什么问题？**

| 问题 | 后果 |
|------|------|
| 按服务名查找慢 | 需要遍历整个 List，O(n) |
| 命名空间隔离 | 无法区分开发/测试/生产环境 |
| 服务分组 | 无法区分同一服务的不同版本 |
| 并发读写 | ArrayList 线程不安全 |

**本质上**：需要一个支持**多维度索引**、**高并发读写**的数据结构。

**解决方案**：三层 Map 结构

```java
// namespace -> group -> serviceName -> Service
Map<String, Map<String, Map<String, Service>>> serviceMap;
```

**为什么是三层？**

| 层级 | 作用 | 示例 |
|------|------|------|
| namespace | 环境隔离 | public（生产）、dev（开发）|
| group | 服务分组 | DEFAULT_GROUP、V1_GROUP |
| serviceName | 服务标识 | order-service、user-service |

**查找过程**：
```
输入：namespace="public", group="DEFAULT_GROUP", serviceName="order-service"
   ↓
serviceMap.get("public")
   ↓
.get("DEFAULT_GROUP")
   ↓
.get("order-service")
   ↓
返回 Service 对象（包含实例列表）
```

**时间复杂度**：O(1)

---

## 第3章：服务实例列表怎么读？

### 问题：服务发现是读多写少，如何优化？

假设一个服务有100个实例，每秒被查询1000次，但每分钟只有1次注册/注销。

**读多写少的场景，如果直接用读写锁？**

```java
ReadWriteLock lock = new ReentrantReadWriteLock();

// 读的时候加读锁
lock.readLock().lock();
try {
    return instances;  // 其他读线程可以并发，但写线程阻塞
} finally {
    lock.readLock().unlock();
}

// 写的时候加写锁
lock.writeLock().lock();
try {
    instances.add(newInstance);  // 所有读写线程都阻塞
} finally {
    lock.writeLock().unlock();
}
```

**问题**：写的时候所有读线程都阻塞，虽然写少，但写的时候还是有性能损失。

**解决方案**：CopyOnWriteArrayList

```java
List<Instance> instances = new CopyOnWriteArrayList<>();

// 读的时候无锁
return instances;  // 直接读，没有任何锁

// 写的时候复制新数组
instances.add(newInstance);  // 复制新数组，写完后原子替换引用
```

**原理**：
```
写操作：
  1. 加锁
  2. 复制原数组 -> 新数组
  3. 新数组添加元素
  4. 原子替换引用（array = newArray）
  5. 解锁

读操作：
  1. 直接读 array（volatile 保证可见性）
  2. 无锁，高并发性能好
```

**代价**：写的时候复制数组，内存开销大。但对于"读99%写1%"的场景，这个代价值得。

---

## 第4章：怎么知道服务还活着？

### 问题：服务实例宕机了，注册中心怎么知道？

**方案一：服务端主动探测**

注册中心定时向每个服务实例发送健康检查请求。

**问题**：
- 服务实例太多，探测压力大
- 探测本身有网络开销
- 探测间隔难以确定（太短压力大，太长发现慢）

**方案二：客户端主动心跳**

服务实例定时向注册中心发送心跳包："我还活着"。

```
服务实例                    注册中心
   |                           |
   |---- 心跳（每5秒）--------->|
   |                           |
   |---- 心跳（每5秒）--------->|
   |                           |
   |     （超过15秒没收到）      | -> 标记不健康
   |                           |
   |     （超过30秒没收到）      | -> 剔除实例
```

**为什么是15秒和30秒？**

| 阈值 | 作用 | 原因 |
|------|------|------|
| 15秒 | 标记不健康 | 网络抖动可能导致短暂超时，不要立即剔除 |
| 30秒 | 剔除实例 | 确认服务真的挂了，避免流量打到死节点 |

**本质上**：心跳是一种"租约"机制，服务实例通过定期续约来维持自己的存活状态。

---

## 第5章：服务端怎么管理海量连接？

### 问题：10万个服务实例同时连上来，服务端怎么承受？

**最简单的方案**：来一个连接就创建一个线程处理。

```java
// 传统 BIO 模式
ServerSocket serverSocket = new ServerSocket(8848);
while (true) {
    Socket socket = serverSocket.accept();  // 阻塞等待连接
    new Thread(() -> handleConnection(socket)).start();  // 每个连接一个线程
}
```

**这会引发什么问题？**

| 问题 | 后果 |
|------|------|
| 线程资源耗尽 | 10万连接 = 10万个线程，内存爆炸 |
| 线程切换开销 | CPU 大量时间花在上下文切换 |
| 怎么知道连接还活着 | TCP 连接断了服务端不一定立即感知 |
| 连接断了怎么清理 | 僵尸连接占用资源 |

**本质上**：需要一个能管理**海量长连接**、能**检测连接健康**、能**自动清理死连接**的机制。

**解决方案：连接管理器（ConnectionManager）**

```java
public class ConnectionManager {
    // 连接表：connectionId -> Connection
    private final ConcurrentHashMap<String, Connection> connections 
        = new ConcurrentHashMap<>();
    
    // 连接活跃时间：connectionId -> lastActiveTime
    private final ConcurrentHashMap<String, Long> activeTime 
        = new ConcurrentHashMap<>();
    
    // 待剔除连接集合
    private final Set<String> outDatedConnections = new HashSet<>();
}
```

**为什么是这三个数据结构？**

| 数据结构 | 作用 | 必要性 |
|----------|------|--------|
| connections | 存储所有连接对象 | 必须，否则找不到连接 |
| activeTime | 记录最后活跃时间 | 必须，用于健康检查 |
| outDatedConnections | 待清理连接集合 | 避免边遍历边删除 |

**连接健康检查算法**：

```
定时任务（每5秒执行一次）：
  1. 遍历 activeTime 中所有连接
  2. 检查最后活跃时间
     - 如果超过20秒无活动：加入 outDatedConnections
  3. 遍历 outDatedConnections
     4. 从 connections 中移除
     5. 从 activeTime 中移除
     6. 关闭 Socket
```

**演进：从 HTTP 到 gRPC**

| 年份 | 协议 | 连接管理方式 |
|------|------|-------------|
| 2020 | HTTP 短轮询 | 每次请求新建连接，无连接管理压力 |
| 2024 | gRPC 长连接 | **必须管理海量长连接**，Nacos 2.x 核心 |
| 2026 | QUIC | 连接迁移，更复杂的连接状态管理 |

**本质上**：连接管理是**长连接架构的基础设施**，没有它就无法支撑大规模集群。

---

## 第6章：订阅是谁发起的？

### 问题：客户端什么时候订阅服务？订阅哪些服务？

**最简单的方案**：客户端启动时，手动订阅需要的服务。

```java
// 手动订阅
nacosClient.subscribe("order-service");
nacosClient.subscribe("user-service");
nacosClient.subscribe("inventory-service");
```

**这会引发什么问题？**

| 问题 | 后果 |
|------|------|
| 需要知道调用了哪些服务 | 代码里到处散落订阅逻辑 |
| 忘记订阅 | 服务变更收不到通知 |
| 连接断了订阅丢失 | 重连后需要重新订阅 |
| 订阅了哪些服务 | 难以追踪和管理 |

**本质上**：需要一种**自动发现订阅需求**、**自动维护订阅关系**的机制。

**方案一：Spring Cloud 自动订阅**

```java
// 通过 @FeignClient 自动发现订阅需求
@FeignClient(name = "order-service")
public interface OrderServiceClient {
    @GetMapping("/order/{id}")
    Order getOrder(@PathVariable Long id);
}

// Spring 启动时自动扫描所有 @FeignClient
// 提取服务名，自动发起订阅
```

**方案二：服务端维护双向索引**

```java
public class ClientServiceIndexesManager {
    /**
     * 服务 -> 订阅者连接列表
     * 用于推送时找到所有订阅者
     */
    private final ConcurrentHashMap<String, Set<String>> 
        serviceSubscriberIndexes = new ConcurrentHashMap<>();
    
    /**
     * 连接 -> 订阅的服务列表
     * 用于连接断开时清理订阅
     */
    private final ConcurrentHashMap<String, Set<String>> 
        subscriberServiceIndexes = new ConcurrentHashMap<>();
}
```

**为什么是双向索引？**

| 索引 | 用途 | 场景 |
|------|------|------|
| service -> subscribers | 推送时找订阅者 | 服务变更时通知谁 |
| subscriber -> services | 断连时清理订阅 | 连接断开时取消哪些订阅 |

**订阅流程**：
```
客户端启动：
  1. 扫描 @FeignClient，提取服务名列表
  2. 对每个服务发送 SubscribeRequest
  3. 服务端更新双向索引
  4. 服务端返回当前服务实例列表
  5. 客户端缓存到本地

服务变更时：
  1. 服务端查 serviceSubscriberIndexes
  2. 得到所有订阅者连接ID
  3. 推送变更给所有订阅者

连接断开时：
  1. 服务端查 subscriberServiceIndexes
  2. 得到该连接订阅的所有服务
  3. 从 serviceSubscriberIndexes 中移除
```

**演进：从显式订阅到声明式订阅**

| 阶段 | 方式 | 示例 |
|------|------|------|
| 手动订阅 | 代码里显式调用 subscribe() | 早期 RPC 框架 |
| 自动订阅 | @FeignClient 注解驱动 | Spring Cloud |
| 声明式订阅 | 配置文件中声明依赖 | Service Mesh |

**本质上**：订阅机制让客户端能**自动感知**需要关注哪些服务，而不需要人肉维护。

---

## 第7章：服务怎么注销？

### 问题：服务实例停止时，怎么告诉注册中心"我要下线了"？

**场景一：正常停止（优雅下线）**

服务实例主动发送注销请求：

```java
// 应用关闭时执行
Runtime.getRuntime().addShutdownHook(new Thread(() -> {
    nacosClient.deregisterInstance("order-service", instance);
}));
```

**问题**：如果注销请求发送失败（网络抖动）怎么办？

**场景二：异常退出（进程崩溃）**

服务实例来不及发送注销请求就挂了。

**问题**：注册中心怎么知道它挂了？

**场景三：网络分区**

服务实例还在运行，但和注册中心的网络断了。

**问题**：注册中心以为它挂了，把它剔除；网络恢复后，服务实例还在运行，但心跳发不出去，最终也被剔除。

**三种注销方式的对比**：

| 方式 | 触发条件 | 实时性 | 可靠性 |
|------|----------|--------|--------|
| **主动注销** | 服务正常停止 | 高 | 中（可能发送失败）|
| **心跳超时** | 服务异常/网络问题 | 低（30秒后）| 高（必然触发）|
| **连接断开** | TCP 连接断开 | 中 | 高（传输层检测）|

**为什么需要主动注销？**

```
场景：服务实例正常重启
  - 发送注销请求
  - 注册中心立即剔除
  - 客户端立即收到推送，不再调用旧实例
  - 新实例启动后注册，客户端收到推送
  
对比只依赖心跳超时：
  - 等待30秒心跳超时
  - 期间流量可能打到即将重启的实例
  - 重启耗时增加30秒
```

**注销流程**：

```
正常注销：
  1. 服务实例发送 DeregisterRequest
  2. 服务端从 ServiceManager 中移除
  3. 更新 lastModifiedTime
  4. 触发推送通知所有订阅者
  5. 返回成功响应

异常注销（心跳超时）：
  1. 服务端检测到 30 秒未收到心跳
  2. 从 ServiceManager 中移除
  3. 触发推送通知所有订阅者
```

**优雅下线的完整流程**：

```
1. 停止接收新请求（如关闭 HTTP 端口）
2. 等待正在处理的请求完成（如 30 秒超时）
3. 发送注销请求到注册中心
4. 等待注销确认（确保推送完成）
5. 关闭应用
```

**数据结构变化**：

```java
// 正常注销
ServiceManager.removeInstance(namespace, group, service, instance);
// 触发事件
EventPublisher.publish(new DeregisterInstanceEvent(service));
// 推送变更
PushService.push(service, subscribers);
```

**为什么注销也要推送？**

```
服务实例注销后：
  - 其他服务不能再调用它
  - 客户端必须立即更新本地缓存
  - 否则可能调用到已下线的实例
```

**生命周期闭环**：

```
注册（上线）→ 心跳（保活）→ 推送（变更通知）→ 注销（下线）
   ↑_________________________________________________|
                    （心跳超时兜底）
```

**本质上**：服务注销是注册的反操作，两者共同构成服务实例的**完整生命周期**。

---

## 第8章：服务变更怎么通知客户端？

### 问题：服务实例变化了，调用方怎么及时知道？

**方案一：客户端定时轮询（Pull）**

```java
while (true) {
    // 每10秒拉一次
    List<Instance> instances = nacosClient.getInstances("order-service");
    updateLocalCache(instances);
    Thread.sleep(10000);
}
```

**问题**：
- 实时性差（最坏情况10秒延迟）
- 无效请求多（大部分时候服务没变）
- 服务端压力大（所有客户端都定时请求）

**方案二：服务端主动推送（Push）**

```
服务注册中心                    客户端
     |                            |
     |<----- 订阅请求 --------------|
     |                            |
     |----- 服务变更推送 ---------->|
     |                            |
     |----- 服务变更推送 ---------->|
```

**问题**：
- 需要维护大量长连接
- 推送失败需要重试
- 推送风暴（服务频繁变更时）

**Nacos的解决方案：推拉结合**

```
正常情况：服务端推送（实时）
推送失败：客户端兜底拉取（可靠）

1. 客户端订阅服务时，建立长连接
2. 服务变更时，服务端通过长连接推送
3. 如果推送失败，客户端在本地缓存过期前主动拉取
4. 服务端故障时，客户端使用本地缓存继续提供服务
```

**推送优化：延迟合并**

```
服务变更频繁时（1秒内变更多次）：
  不立即推送，而是延迟500ms
  500ms内的多次变更合并为一次推送
  减少推送次数，降低服务端压力
```

---

## 第9章：服务端挂了怎么办？

### 问题：注册中心本身故障了，服务还能调用吗？

**单点故障的风险**：
- 注册中心宕机，所有服务无法发现对方
- 整个系统瘫痪

**方案一：注册中心集群**

部署多个 Nacos 节点，数据互相复制。

**但还有问题**：
- 网络分区时，客户端连不上任何节点怎么办？
- 短暂断网后恢复，怎么知道服务变了什么？

**方案二：客户端本地缓存 + 故障转移**

```java
class ServiceInfoHolder {
    // 内存缓存
    Map<String, ServiceInfo> serviceInfoMap;
    
    // 磁盘缓存（故障转移用）
    File failoverDir;
}
```

**故障转移流程**：
```
1. 正常时：客户端从服务端获取服务列表，写入内存 + 磁盘
2. 服务端故障：客户端检测到连接断开
3. 故障转移：从磁盘缓存读取服务列表继续使用
4. 恢复后：重新从服务端同步最新数据
```

**本质上**：注册中心是"权威数据源"，但客户端要有"本地副本"作为兜底。

---

## 第10章：多节点数据怎么一致？

### 问题：Nacos集群中，一个服务注册到节点A，节点B怎么知道？

**最简单的方案**：所有节点共享一个数据库

**问题**：
- 数据库成为瓶颈
- 读写性能受限
- 不适合高并发场景

**方案：最终一致性（Distro协议）**

```
核心思想：
1. 每个服务由特定节点负责写（根据服务名哈希）
2. 写完后异步同步给其他节点
3. 读的时候直接读本地（可能读到旧数据，但最终一致）

写流程：
  客户端 -> 任意节点 -> 计算负责节点 -> 转发写请求 -> 异步广播

读流程：
  客户端 -> 任意节点 -> 直接返回本地数据
```

**为什么是最终一致而不是强一致？**

| 特性 | 最终一致（Distro） | 强一致（Raft） |
|------|-------------------|---------------|
| 性能 | 高（异步复制） | 低（同步复制） |
| 可用性 | 高（分区容忍） | 低（脑裂时不可用） |
| 复杂度 | 低 | 高 |
| 适用场景 | 服务注册（临时数据） | 配置中心（持久数据） |

**服务注册用最终一致的原因是**：
- 服务实例信息是临时性的（有生命周期）
- 偶尔读到旧数据没关系（心跳会刷新）
- 追求高可用和高性能

---

## 第11章：一个服务由哪个节点负责？

### 问题：Distro协议中，怎么决定"order-service"由哪个节点负责写？

**方案一：轮询**

按顺序分配给各个节点。

**问题**：节点扩容/缩容时，大量服务需要重新分配。

**方案二：哈希取模**

```java
int nodeIndex = hash(serviceName) % nodeCount;
```

**问题**：节点数量变化时，几乎所有服务都要重新分配（缓存失效）。

**方案三：一致性哈希**

```
把节点和数据都映射到一个哈希环上：

      0 ------------------------------------------------ 2^32
      |                                                  |
      |    node1        node2        node3              |
      |      ●            ●            ●                 |
      |                                                  |
      |   serviceA   serviceB   serviceC   serviceD     |
      |      ↑          ↑          ↑          ↑          |
      +--------------------------------------------------+

serviceA 顺时针找，找到 node1 负责
serviceB 顺时针找，找到 node2 负责
...

加入 node4：
      0 ------------------------------------------------ 2^32
      |                                                  |
      |    node1   node4   node2        node3           |
      |      ●       ●      ●            ●              |
      |                                                  |
      |   serviceA   serviceB   serviceC   serviceD      |
      |      ↑                   ↑          ↑           |
      +--------------------------------------------------+

只有 serviceB 从 node2 迁移到 node4，其他不变
```

**优势**：节点变化时，只有少部分数据需要迁移。

---

## 第12章：为什么需要配置中心？

### 问题：配置变更必须重启服务吗？

传统方式：配置写在配置文件里，改配置需要重启服务。

**痛点**：
- 重启耗时（几十秒到几分钟）
- 重启期间服务不可用
- 批量重启风险高

**解决方案：配置中心**

```
配置中心（Nacos Config）
     |
     | 推送配置变更
     ↓
┌─────────┐  ┌─────────┐  ┌─────────┐
│ 订单服务 │  │ 用户服务 │  │ 库存服务 │
└─────────┘  └─────────┘  └─────────┘
```

**关键区别**：

| 特性 | 服务注册 | 配置中心 |
|------|----------|----------|
| 数据特性 | 动态变化（实例上下线） | 相对静态 |
| 一致性要求 | 最终一致 | 强一致 |
| 历史版本 | 不需要 | 需要（回滚用） |
| 协议 | Distro（AP） | Raft（CP） |

**配置中心用强一致的原因是**：
- 配置变更不频繁
- 配置错误可能导致严重故障，必须保证所有节点看到一致
- 可以牺牲一点性能换取一致性

---

## 总结：Nacos 解决了哪些问题？

| 问题 | 解决方案 | 核心设计 |
|------|----------|----------|
| 服务怎么找？ | 服务注册中心 | 三层 Map 索引 |
| 怎么找到注册中心？ | 分层配置体系 | JVM参数→环境变量→配置文件 |
| 服务存在哪？ | 服务端存储 | namespace→group→service |
| 读多写少怎么优化？ | CopyOnWrite | 读无锁，写复制 |
| 服务宕机怎么发现？ | 心跳机制 | 15秒标记，30秒剔除 |
| 海量连接怎么管理？ | ConnectionManager | 连接表 + 活跃时间 + 定时清理 |
| 订阅关系怎么维护？ | 双向索引 | 服务→订阅者 + 订阅者→服务 |
| 服务怎么注销？ | 主动注销 + 心跳超时 | 优雅下线 + 异常兜底 |
| 服务变更怎么通知？ | 推拉结合 | 长连接推送 + 本地缓存兜底 |
| 注册中心挂了怎么办？ | 客户端缓存 | 内存 + 磁盘双缓存 |
| 多节点数据怎么一致？ | Distro协议 | 哈希分片 + 异步同步 |
| 节点变化怎么平滑？ | 一致性哈希 | 虚拟节点，少量迁移 |
| 配置怎么动态更新？ | 配置中心 | Raft强一致 + 主动推送 |

---

## 手写检查清单（每章问自己）

- [ ] 第0章：为什么不用 DNS 做服务发现？
- [ ] 第1章：为什么配置要分层？只用配置文件行不行？
- [ ] 第2章：为什么要三层 Map？两层行不行？
- [ ] 第3章：CopyOnWrite 的缺点是什么？
- [ ] 第4章：如果网络抖动导致心跳丢失，会误判吗？
- [ ] 第5章：10万连接和100万连接，瓶颈分别在哪？
- [ ] 第6章：订阅关系在连接断开后怎么恢复？
- [ ] 第7章：主动注销失败了怎么办？
- [ ] 第8章：纯推模式和纯拉模式各有什么问题？
- [ ] 第9章：本地缓存多久更新一次？怎么保证不脏读？
- [ ] 第10章：AP 和 CP 的本质区别是什么？
- [ ] 第11章：一致性哈希的虚拟节点有什么作用？
- [ ] 第12章：配置中心为什么用 Raft 而不是 Distro？
